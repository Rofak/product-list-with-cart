module.exports = {
    
    extends: [
      '@nuxtjs',      // Nuxt.js specific ESLint rules
      'eslint:recommended',
      'plugin:nuxt/recommended',
      'airbnb-base'   // You can choose other presets (standard, prettier, etc.)
    ],
    rules: {
      // Add custom rules or override specific rules here
    }
  };
  